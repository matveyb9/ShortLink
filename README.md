# URL Shortener Service

Сервис для сокращения ссылок с поддержкой PostgreSQL и MySQL.

## Возможности

- 🔗 Сокращение длинных URL в короткие 6-значные коды
- 🎨 Современный веб-интерфейс
- 📊 Подсчёт кликов
- 🔌 REST API для интеграции
- 🗄️ Поддержка PostgreSQL и MySQL
- ⚡ Готов к деплою на Timeweb Cloud Apps

## Структура проекта

```
url-shortener/
├── app.py              # Основное приложение
├── requirements.txt    # Зависимости Python
├── .env.example       # Пример файла переменных окружения
└── README.md          # Документация
```

## Установка

### 1. Клонируйте репозиторий

```bash
git clone <your-repo-url>
cd url-shortener
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите свои настройки:

```env
DB_TYPE=postgresql
DB_HOST=localhost
DB_PORT=5432
DB_NAME=urlshortener
DB_USER=postgres
DB_PASSWORD=your_password
BASE_URL=http://localhost:5000
```

### 5. Создайте базу данных

**PostgreSQL:**
```sql
CREATE DATABASE urlshortener;
```

**MySQL:**
```sql
CREATE DATABASE urlshortener CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 6. Запустите приложение

```bash
python app.py
```

Приложение будет доступно по адресу `http://localhost:5000`

## Деплой на Timeweb Cloud Apps

### 1. Создайте приложение

В панели Timeweb Cloud Apps создайте новое приложение Python.

### 2. Подключите базу данных

Создайте базу данных PostgreSQL или MySQL в Timeweb Cloud и получите данные для подключения.

### 3. Настройте переменные окружения

В настройках приложения добавьте переменные окружения:

- `DB_TYPE` - тип БД (postgresql или mysql)
- `DB_HOST` - хост БД
- `DB_PORT` - порт БД
- `DB_NAME` - имя БД
- `DB_USER` - пользователь БД
- `DB_PASSWORD` - пароль БД
- `BASE_URL` - ваш домен (например, https://short.example.com)

### 4. Деплой

Загрузите файлы проекта или подключите Git-репозиторий.

Timeweb автоматически установит зависимости из `requirements.txt` и запустит приложение.

## API Documentation

### Сократить URL

**POST** `/api/shorten`

Request body:
```json
{
  "url": "https://example.com/very/long/url"
}
```

Response (201):
```json
{
  "short_url": "https://your-domain.com/aB3dE5",
  "shortcode": "aB3dE5",
  "original_url": "https://example.com/very/long/url"
}
```

### Получить информацию о ссылке

**GET** `/api/info/<shortcode>`

Response (200):
```json
{
  "shortcode": "aB3dE5",
  "original_url": "https://example.com/very/long/url",
  "created_at": "2024-01-01T12:00:00",
  "clicks": 42
}
```

## Маршруты

- `/` - Главная страница с веб-интерфейсом
- `/<shortcode>` - Переадресация по короткой ссылке
- `/api/shorten` - API для создания короткой ссылки
- `/api/info/<shortcode>` - API для получения информации о ссылке

## Технологии

- **Backend:** Flask (Python)
- **Database:** PostgreSQL / MySQL
- **Frontend:** Vanilla JavaScript, HTML5, CSS3
- **Server:** Gunicorn

## Безопасность

- Валидация URL перед сохранением
- Защита от SQL-инъекций через параметризованные запросы
- CORS настроен для работы с API
- Все чувствительные данные хранятся в переменных окружения

## Лицензия

MIT
